"""Model for training and predict information from data."""

from __future__ import annotations

import pandas as pd


def train(df: pd.DataFrame) -> dict[str, float | pd.Series]:
    """Train a ML model with given dataframe."""
    return {}
